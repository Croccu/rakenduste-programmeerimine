import { createTheme } from "@mui/material/styles"

export const theme = createTheme({
  palette: {
    mode: "light",                // vali "light" või "dark"
    primary: { main: "#0ea5e9" },
    secondary: { main: "#e6e6e6" }
  }
})
